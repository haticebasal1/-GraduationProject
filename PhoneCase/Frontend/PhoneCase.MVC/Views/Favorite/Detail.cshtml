@model ProductDto
@{
    ViewData["Title"] = "Details";
}
<div class="container" style="margin-top: 120px;">
    <div class="row">
        <div class="col-md-6">
            <img src="~/ui/img/@Model.ImageUrl" class="img-fluid rounded shadow" alt="@Model.Name" />
        </div>
        <div class="col-md-6">
            <h2>@Model.Name</h2>
            <p class="text-muted">@Model.Categories</p>
            <p class="fw-bold text-primary">@Model.Price ₺</p>
            <p>@Model.Properties</p>
        </div>
    </div>
</div>
